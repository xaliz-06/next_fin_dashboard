"use client";

import CountUp from "react-countup";
export { CountUp };

// Hack to work around React-Countup Package in Next.js as the package is quite old and this seem to be the only thing that works
